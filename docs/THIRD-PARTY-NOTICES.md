# Third-Party Notices

This project itself is released under [The Unlicense](../LICENSE).
It bundles or invokes no third-party software — this package has no runtime (`require`) dependencies, only PHP 8.3+ itself.

## Composer dependencies

No runtime dependency table applies (see above).
Development-only dependencies (`require-dev`) are not distributed with the package and carry no attribution obligation for consumers; full list with versions and licenses: run `composer licenses`.

## Notes

This is not legal advice.
